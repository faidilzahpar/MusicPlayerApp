<Window x:Class="MusicPlayerApp.Views.CreatePlaylistDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Playlist"
        Width="360"
        Height="180"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True" Background="Transparent">
    <Border CornerRadius="12" Padding="20" Background="#1B2338">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Create new playlist"
                       FontSize="18"
                       FontWeight="Bold"
                       Foreground="White"
                       Margin="0 0 0 12"/>

            <TextBox x:Name="PlaylistNameBox"
                     Grid.Row="1"
                     Height="38"
                     FontSize="14"
                     Foreground="#1B2338"
                     Background="White"
                     BorderThickness="0"
                     Padding="10"
                     CaretBrush="#1B2338" Text="My Playlist"
                     VerticalContentAlignment="Center"/>
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0 16 0 0">

                <Button Content="Cancel"
                        Width="80"
                        Margin="0 0 10 0"
                        Background="#2A3550" Foreground="White"
                        BorderThickness="0"
                        Click="Cancel_Click"/>

                <Button Content="Create"
                        Width="80"
                        Background="#5CD1FF" Foreground="#1B2338" BorderThickness="0"
                        FontWeight="SemiBold"
                        Click="Create_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>